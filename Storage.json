const discord = require("discord.js");
const bot = new discord.Client();

const TOKEN = "secret"
const PREFIX = "!"

function generateHex() {
    return "#" + Math.floor(Math.rando() * 16777215).toString(16);
}

var bot = new Discord.Client();

bot.on("ready", function() {
    console.log("Ready");
});

bot.on("guildMemberAdd", function(member) {
    member.guild.channels.find("name", "general").sendMessage(member.toString() + " Welcome to  Fraudlyfe"
});

bot.on("message", function(message) {
    if (message.author.equals(bot.user)) return;

    if (!message.content.startsWith(PREFIX)) return;

    var args = message.content.substring(PREFIX.length).split(" ");

    switch (args[0]) {
        case "ping":
            message.channel.sendMessage("Pong!");
            break;
        case "info":
            message.channel.sendMessage("I'm a super bot created by Rashed");
            break;
        case "embed":
           var help = new Discord.RichEmbed()
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .addField("Ping", "Pong", true)
               .setColor(0x00FFFF)
               .setFooter("Still adding Commands - Yu Xiu")
               .setThumbnail(message.author.avatarURL)
            message.channel.sendEmbed(help);
            break;
        case "mames":
           message.channel.sendMessage(message.author.toString() + " type g!embed for memes!");
           break;
         default:
            message.channel.sendMessage("Invalid command");
    }          
});

bot.login(TOKEN);﻿
